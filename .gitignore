# rust build artifacts
/target

# python (legacy reference implementation) artifacts
__pycache__/
*.pyc

# personal / local dev config (not part of the project)
opencode.json
.odin/

# runtime state & caches live in ~/.config/tokicat/ (outside the repo)
